{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "https://served-accessible-doors-linear.trycloudflare.com/$1"
    }
  ]
}
